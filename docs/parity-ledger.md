# Parity Ledger

| Upstream path/function | Local path/function | Status | Parity proof | Known deviations |
| --- | --- | --- | --- | --- |
| `libfaad/*.c` LC decoder slice | `internal/faad2ccgo/faad2_*.go` | generated translation | committed vector SHA-256 tests plus native FAAD2 oracle byte comparison | Generated for `darwin/arm64` and `linux/arm64`; source macros are `LC_ONLY_DECODER` and `DISABLE_SBR`; oracle builds use `-ffp-contract=off`. |
| `libfaad/decoder.c:NeAACDecOpen` | `decoder_purego.go:newPureDecoder` | wrapped | integration test | Go wrapper fixes output to interleaved S16 and disables implicit SBR upsampling. |
| `libfaad/decoder.c:NeAACDecInit` | `decoder_purego.go:initADTS` | wrapped | integration test | Public ADTS path rejects frames with multiple raw data blocks before calling FAAD2. |
| `libfaad/decoder.c:NeAACDecInit2` | `decoder_purego.go:initRaw` | wrapped | raw config tests plus compile gate | Public raw path accepts AAC-LC AudioSpecificConfig only. |
| `libfaad/decoder.c:NeAACDecDecode` | `decoder_purego.go:decodeInto` | wrapped | native FAAD2 oracle byte comparison | Samples are appended to caller-owned output before the next decode call. |
| `libfaad/error.c:NeAACDecGetErrorMessage` | `decoder_purego.go:frameError` | wrapped | integration error path coverage | Error text is surfaced with the FAAD2 code. |
| `include/neaacdec.h` output structs | `internal/faad2ccgo.NeAACDecFrameInfo` | generated translation | compile gate | Field names use generated `F...` prefixes. |
| MPEG-4 AudioSpecificConfig parsing | `config.go:ParseAudioSpecificConfig` | hand-translated subset | unit tests | AAC-LC/SBR/PS detection only; unsupported extensions are rejected by the public facade. |
| ADTS fixed/variable header parsing | `adts.go:ParseADTSHeader` | hand-translated subset | unit tests, integration fixture | Rejects multiple raw data blocks because the public decoder is one access unit at a time. |
| ADTS header emission | `adts.go:AppendADTSHeader` | hand-translated subset | unit tests | Writes CRC-less ADTS headers only. |
| FLV/RTMP AAC audio tag parsing | `flv.go:ParseFLVAudioTag`, `flv.go:FLVAACDecoder` | Go streaming facade | zero-allocation parser test, control-transition tests, committed vector SHA-256 tests through synthesized RTMP/FLV payloads | Handles FLV/RTMP audio message bodies only; RTMP chunking and full FLV file tag headers stay outside this package. |
| FLV/RTMP AAC audio tag emission | `flv.go:AppendFLVAACSequenceHeader`, `flv.go:AppendFLVAACRawTag` | Go streaming facade | unit tests round-trip through `ParseFLVAudioTag` and `ParseAudioSpecificConfig` | Emits FLV/RTMP audio message bodies only; muxers still own RTMP chunking and FLV file tag headers. |
| FDK-AAC encoder source truth | `third_party/fdk-aac`, `docs/encoder-reference.md` | pinned upstream reference | `scripts/build_fdkaac_oracle.sh` builds the native `aac-enc` oracle | The public encoder facade is not complete yet; FDK-AAC is a translation/oracle source and not a Go runtime dependency. |
| FDK AAC-LC bandwidth, bitrate-mode, channel-map, and element-bit control | `internal/fdkaac/bandwidth.go`; `internal/fdkaac/channel_map.go` | source-shaped partial port | Source-shaped `FDKaacEnc_DetermineBandWidth` vectors for CBR/VBR table selection, proposed-bandwidth clipping, low-delay fixed-point interpolation, LFE-excluded effective-channel handling, invalid channel bitrate and unsupported mode controls; `FDKaacEnc_GetVBRBitrate` and `FDKaacEnc_AdjustVBRBitrateMode` vectors; `FDKaacEnc_DetermineEncoderMode`, `FDKaacEnc_InitChannelMapping`, and `FDKaacEnc_InitElementBits` vectors covering MPEG/WAV/WG4 order, relative-bit shares, element instance tags, 5.1/6.1/7.1 maps, 7.1 aliases, per-element bitrate, max-bit budgets, and LFE reservoir exclusion; no-cgo compile gate and zero steady-state allocation check | Covers FDK's encoder-control bandwidth selection, VBR mode lowering, channel-mode selection, channel mapping, and element-bit initialization before psychoacoustic/QC frame processing. Full public encoder initialization, bitrate limiting, SFB table selection, psychoacoustic init, quantizer iteration, and oracle byte parity are not claimed yet. |
| FDK AAC-LC encoder config and QC budget setup | `internal/fdkaac/aacenc.go:AACEncConfig`, `AACEncInitState`, `FDKaacEncAacInitDefaultConfig`, `FDKaacEncCalcBitsPerFrame`, `FDKaacEncCalcBitrate`, `FDKaacEncLimitBitrate`, `FDKaacEncInitCheckAncillary`, `FDKaacEncPrepareQCInitFromConfig`, `FDKaacEncEncBitresToTpBitres` | source-shaped partial port | Source-shaped default-config vector, frame-bit/bitrate vectors, low/high bitrate-limit vectors, ancillary-rate vectors, CBR and VBR multi-element QC-budget vectors, config-to-`FDKaacEncQCInit` chaining vector, transport bit-reservoir signaling vectors, malformed-control tests, no-cgo compile gate, and zero steady-state allocation check | Covers FDK's deterministic `aacenc.cpp` control math that feeds QC initialization: static transport-bit callback use, bitrate clamp math, max ancillary bytes, bandwidth handoff, mean-PE and max-bit-factor calculation, bit-reservoir mode selection, bitrate-mode mapping, afterburner `invQuant`, and transport reservoir signaling. Psychoacoustic kernel initialization, TNS/PNS/MS parameter setup, public encoder facade, actual frame encoding, and oracle byte parity are not claimed yet. |
| FDK bitstream writer primitive | `internal/fdkaac/bitstream.go` | source-shaped partial port | Native FDK v2.0.3 byte-vector probe, unit tests, ring-buffer wrap tests, and zero steady-state allocation check | Writer-side bitstream subset only; reader/backward-write paths are not claimed yet. |
| FDK fixed-point primitive subset | `internal/fdkaac/fixpoint.go` | source-shaped partial port | Native FDK v2.0.3 arm64 vector probe for constants, conversions, `fixmul*`, `fPow2*`, `fixnorm*`, `scaleValue*`, `SATURATE_*`, `SATURATE_*_ALT`, and zero steady-state allocation check | Arithmetic contract follows the pinned arm64 FDK inline paths where they differ from generic fallbacks; vector coverage is primitive-level, not encoder-quality proof. |
| FDK multiply-add primitive subset | `internal/fdkaac/fixmadd.go` | source-shaped partial port | Native FDK v2.0.3 arm64 vector probe for `fixmadd*`, `fixmsub*`, bit-exact div2 helpers, `fixpadd*`, and zero steady-state allocation check | Primitive-level proof only; no DSP kernel or encoder quality claim yet. |
| FDK scale-vector primitive subset | `internal/fdkaac/scale.go` | source-shaped partial port | Native FDK v2.0.3 arm64 vector probe for DBL/SGL/PCM scaling, saturated DBL/SGL scaling, factor scaling, scalefactor scans, and zero steady-state allocation check | `scaleValuesWithFactor` follows the pinned arm64 override; zero-scale SGL dst/src copy is bounded to the Go slice length instead of reproducing FDK's oversized `FDKmemmove` byte count. |
| FDK complex multiply primitive subset | `internal/fdkaac/cplxmul.go` | source-shaped partial port | Native FDK v2.0.3 arm64 vector probe for `cplxMultDiv2`, `cplxMultSubDiv2`, and `cplxMult` DBL/SGL overloads, including steady-state allocation checks | `cplxMultDiv2` follows the pinned arm64 combined-product path for DBL and DBL/SGL inputs; Go exposes the C++ overloads as explicit typed helpers, so true SGL-output and promoted DBL-output paths are separate. |
| FDK fixed short FFT subset | `internal/fdkaac/fft.go` | source-shaped partial port | Native FDK v2.0.3 arm64 vector probe for `fft_4` and `fft_8`, plus zero steady-state allocation check | Short in-place kernels only; full `fft`, `ifft`, radix-2 `dit_fft`, scramble, MDCT, and twiddle table ports are not claimed by this row. |
| FDK radix-2 FFT subset | `internal/fdkaac/fft.go` | source-shaped partial port | Source-derived vectors from pinned `scramble` and arm64 `SINETABLE_16BIT` `dit_fft` equations, `internal/fdkaac` runtime tests under `GOOS=js GOARCH=wasm`, compile-only package checks, `go vet`, and no-cgo/native-loader scan | Fresh native vectors and native Mach-O `go test` execution were not available because newly built Mach-O binaries were stuck in `dyld` before `main`; full `fft`, `ifft`, MDCT, and non-512 sine/twiddle tables are not claimed yet. |
| FDK sine ROM table for radix FFT | `internal/fdkaac/rom.go:SineTable512`, `internal/fdkaac/fft.go:DITFFT512` | source-shaped partial port | Source-derived checksum and sample-index tests for `SineTable512`, length 64/128/256/512 radix FFT output hashes, unsupported-length transition test, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers the pinned 16-bit `SineTable512` quarter-wave table and FDK's 64/128/256/512 `dit_fft` call shape only; 480/384/80 tables, MDCT twiddles, full `fft`, and `ifft` are not claimed by this row. |
| FDK FFT dispatcher subset | `internal/fdkaac/fft.go:FFT2`, `FFT` | source-shaped partial port | Source-derived length-2 vectors, exact dispatcher vectors for 2/4/8, source-derived length 64/128/256/512 radix hashes, scale-factor transition tests, unsupported-length transition tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Supports only the branches whose kernels and tables are already ported: 2, 4, 8, 64, 128, 256, and 512. Mixed-radix lengths, the 16/32 unrolled kernels, full public `fft`, and `ifft` are not claimed yet. |
| FDK radix-2 DCT sine ROM table | `internal/fdkaac/rom.go:SineTable1024` | source-shaped partial port | Source-derived checksum and sample-index tests, a relation test proving `SineTable512` equals every second entry, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers only the pinned 16-bit `SineTable1024` quarter-wave table selected by `dct_getTables` for radix-2 DCT-IV lengths. DCT-IV is covered separately; MDCT folding/windowing and non-radix sine tables are not claimed by this row. |
| FDK AAC-LC window slope ROM subset | `internal/fdkaac/window.go:FDKGetWindowSlope`, AAC-LC sine/KBD window tables | source-shaped partial port | Source-derived checksum and sample-index tests for 1024/960 and 128/120 sine/KBD window tables, selector tests for long and short AAC-LC lengths, `shape & 1` behavior, unsupported-length transitions, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers AAC-LC long/short slope tables used by the encoder transform path. Other FDK window tables, low-overlap AAC-LD/ELD behavior, full `windowSlopes`, and MDCT folding/windowing are not claimed yet. |
| FDK radix-2 DCT-IV subset | `internal/fdkaac/dct.go:dctGetTables`, `DCTIV` | source-shaped partial port | Source-derived output hashes for lengths 128 and 1024, table-selection tests for FDK sine/window twiddles, scale-factor transition tests, unsupported-length transition tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Supports only the radix-2 AAC-LC DCT-IV lengths whose FFT/table dependencies are already ported: 128 and 1024. The 120/960 mixed-radix path, DCT-II/III, DST-IV, and MDCT folding/windowing are not claimed yet. |
| FDK AAC-LC MDCT transform subset | `internal/fdkaac/mdct.go:MDCTBlock`, `FDKaacEncTransformReal` | source-shaped partial port | Source-derived output hashes for long and short AAC-LC transforms, long/start/short/stop transition hashes with sine/KBD window-state changes, exact scale-factor tests, unsupported mixed-radix transition tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Supports the radix-2 AAC-LC transform path for frame length 1024 and short length 128. The 960/120 mixed-radix path, inverse MDCT, AAC-LD/ELD transforms, block switching decisions, psychoacoustic analysis, quantization, and bitstream coding are not claimed yet. |
| FDK AAC-LC block switching subset | `internal/fdkaac/block_switch.go:FDKaacEncInitBlockSwitching`, `FDKaacEncBlockSwitching`, `FDKaacEncSyncBlockSwitching` | source-shaped partial port | Source-derived state hashes for LC attack/no-attack frame transitions, IIR-filtered and unfiltered window energy hashes, LFE and low-delay controls, stereo common-window grouping sync vectors, unsupported-state tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers FDK's fixed 16-bit PCM block-switching decision path and stereo grouping sync used before AAC-LC transform analysis. Psychoacoustic thresholding, TNS, MS stereo, quantization, rate control, and bitstream coding are not claimed yet. |
| FDK log-data math subset | `internal/fdkaac/fixlog.go` | source-shaped partial port | Source-derived vectors for `fLog2`, `CalcLdData`, and `LdDataVector`, in-place vector test, invalid transition tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers the LD64 log-energy representation used by AAC-LC psychoacoustic energy paths. Pow and broader fixpoint math helpers are not claimed yet. |
| FDK inverse-LD math subset | `internal/fdkaac/fixlog.go:CalcInvLdData` | source-shaped partial port | Source-derived vectors for the FDK `exp2_tab_long`/`exp2w_tab_long`/`exp2x_tab_long` lookup and interpolation path, ROM edge checks, underflow/saturation cases, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers inverse LD64 conversion used by AAC-LC scale-factor and relevant-line paths. Pow, quantizer powers, and broader fixpoint math helpers are not claimed yet. |
| FDK square-root math subset | `internal/fdkaac/fixsqrt.go` | source-shaped partial port | Source-derived vectors for the generic FDK `invSqrtNorm2` table/interpolation path and `sqrtFixp`, invalid input tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers the deterministic fixed-point square-root primitive used by AAC-LC encoder form-factor and intensity paths. Pow, quantizer powers, and CPU-specific float shortcuts are not claimed by this row. |
| FDK AAC-LC band-energy subset | `internal/fdkaac/band_nrg.go` | source-shaped partial port | Source-derived vectors for `FDKaacEnc_CalcSfbMaxScaleSpec`, `FDKaacEnc_CheckBandEnergyOptim`, `FDKaacEnc_CalcBandEnergyOptimLong`, `FDKaacEnc_CalcBandEnergyOptimShort`, and `FDKaacEnc_CalcBandNrgMSOpt` with log-data output, invalid transition tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers long/short scalefactor-band energies, pre-shift energy checking, and mid/side energy preparation after MDCT. The later psychoacoustic threshold, spreading, PE, TNS, quantization, and bitstream coding stages are not claimed yet. |
| FDK scalefactor delta bit-count subset | `internal/fdkaac/bit_cnt.go:FDKaacEncBitCountScalefactorDelta` | source-shaped partial port | Source-derived vectors for the FDK scalefactor Huffman length table, selected deltas across the legal `[-60, 60]` range, invalid delta transitions, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers scalefactor delta bit lengths used by PE and dynamic bit counting. Codeword emission is covered by the separate Huffman emission row. |
| FDK spectral Huffman bit-count subset | `internal/fdkaac/huff_ltab.go`, `internal/fdkaac/bit_cnt.go:FDKaacEncBitCount`, `FDKaacEncCountValues` | source-shaped partial port | Mechanically derived checksums for the FDK spectral Huffman length ROMs, source-derived vectors across zero, LAV1/2/4/7/12/15, escape-count, short-band zero-tail bit counts, committed-codebook count-values, invalid control transitions, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers spectral Huffman bit lengths used by quantized-spectrum codebook search and fixed-codebook recounting. Go validates widths and slice lengths explicitly, and materializes underfilled bit-count groups with a zero tail instead of reading outside the logical scalefactor band. Section/codebook search, noiseless section side-info, and final element assembly are not claimed by this row. |
| FDK Huffman codeword emission subset | `internal/fdkaac/huff_ctab.go`, `internal/fdkaac/bit_cnt.go:FDKaacEncCodeValues`, `FDKaacEncCodeScalefactorDelta` | source-shaped partial port | Mechanically derived checksums for the FDK spectral/scalefactor codeword ROMs, source-derived byte vectors for codebooks 0-11 including pair-width codebooks and escape extensions, scalefactor-delta byte vectors, invalid transition tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers direct Huffman codeword emission into the existing FDK-shaped bitstream writer. Go validates widths and slice lengths explicitly. Section search and dynamic bit counting are covered separately; TNS/PNS/MS integration, bit reservoir control, and final raw-AAC element assembly are not claimed by this row. |
| FDK dynamic noiseless bit-count subset | `internal/fdkaac/dyn_bits.go`, `internal/fdkaac/sf_estim.go:QCOutChannel.SectionData` | source-shaped partial port | Source-derived long-window, ER-VCB11, short-grouped, PNS, and intensity vectors for `FDKaacEnc_dynBitCount`, including section layout hashes, Huffman/side-info/scalefactor/noise totals, invalid transition tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers FDK's greedy section/codebook search, section side-info accounting, scalefactor skip counting, and PNS noise-energy bit counting for quantized AAC-LC channel data. Go uses a caller-owned fixed scratch state sized to the package's grouped-SFB maximum and validates slices/offsets explicitly. Final bitstream element assembly and rate-control iteration around the counter are not claimed yet. |
| FDK channel bitstream writer helper subset | `internal/fdkaac/bitenc.go` | source-shaped partial port | Source-derived byte vectors for `FDKaacEnc_encodeGlobalGain`, `FDKaacEnc_encodeIcsInfo`, `FDKaacEnc_encodeSectionData`, `FDKaacEnc_encodeScaleFactorData`, `FDKaacEnc_encodeSpectralData`, `FDKaacEnc_encodeMSInfo`, `FDKaacEnc_encodeTnsDataPresent`, `FDKaacEnc_encodeTnsData`, `FDKaacEnc_encodePulseData`, `FDKaacEnc_encodeGainControlData`, `FDKaacEnc_writeExtensionPayload`, `FDKaacEnc_writeDataStreamElement`, and `FDKaacEnc_writeExtensionData`, including a combined long-channel payload sequence, GA fill-element and DSE payload vectors, ER/ELD extension branches, malformed transition tests, and zero steady-state allocation checks under `GOOS=js GOARCH=wasm` | Covers channel-local writer helpers over already-built `SectionData`, quantized spectrum, MS mask decisions, TNS info, and extension payload packing/counting. Transport CRC hooks, access-unit finalization, and rate-control iteration are not claimed yet. |
| FDK AAC-LC channel element writer subset | `internal/fdkaac/bitenc.go:FDKaacEncGetBitstreamElementList`, `FDKaacEncChannelElementWrite` | source-shaped partial port | Source-derived sequence-table pins for AAC-LC SCE/CPE normal syntax branches, SCE and common-window CPE byte vectors assembled through the existing global-gain, ICS, MS, section, scalefactor, TNS-present, pulse/gain-control, and spectral writers; `minCnt` static-demand vectors for SCE TNS suppression and CPE MS suppression; malformed bit-count error paths, invalid transition tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers mono/LFE and stereo CPE normal AAC-LC element traversal over already-produced psychoacoustic and quantized channel outputs, plus FDK's minimum-static syntax skeleton used by low-reservoir frame control. ER/scalable syntax, CRC state hooks, fill/final access-unit packing, rate-control iteration, and the public encoder facade are not claimed yet. |
| FDK AAC-LC raw AU bitstream writer subset | `internal/fdkaac/bitenc.go:FDKaacEncWriteBitstream`, `FDKaacEncByteAlignment`; `internal/fdkaac/adj_thr.go:QCOut.Extension`, `QCOutElement.Extension` | source-shaped partial port | Source-shaped SCE raw-AU byte vector, element/global extension ordering vector, written-bit mismatch controls, invalid element/control transitions, no-cgo compile gate, and zero steady-state allocation check | Covers FDK's normal AAC-LC top-level channel-element traversal, element-associated extension payloads, global extension payloads, synthetic fill-data entry, `ID_END`, explicit alignment bits, and final written-bit verification over already-prepared QC/psy outputs. ER/scalable syntax, transport wrapper callbacks, multi-subframe access-unit assembly, and the public encoder facade are not claimed yet. |
| FDK AAC-LC QC lifecycle state linking | `internal/fdkaac/qc_lifecycle.go:QCState`, `QCOutState`, `FDKaacEncQCNew`, `FDKaacEncQCOutNew`, `FDKaacEncQCOutInit`; `internal/fdkaac/adj_thr.go:QCOut.PQCOutChannels`, `QCOut.QCElement` | source-shaped partial port | Source-shaped fixed-state vectors for `FDKaacEnc_QCNew`, `FDKaacEnc_QCOutNew`, and `FDKaacEnc_QCOutInit`; 5.1 WAV channel-to-element pointer traversal; config-to-QCInit lifecycle handoff; malformed control tests; no-cgo compile gate; and zero steady-state allocation check | Covers the FDK QC owner/pointer setup that wires threshold state, element-bit state, QC output channels, and QC output elements before `FDKaacEnc_QCInit` and `FDKaacEnc_QCMain`. Dynamic C RAM scratch pointers are represented by existing caller-owned Go scratch structs. Psychoacoustic owner allocation, transport owner allocation, full frame encode orchestration, public encoder facade, and oracle byte parity are not claimed yet. |
| FDK AAC-LC QC initialization | `internal/fdkaac/qc_main.go:QCInit`, `FDKaacEncQCInit`; `internal/fdkaac/adj_thr.go:FDKaacEncInitAdjThrState`, `FDKaacEncInitATSElement`; `internal/fdkaac/channel_map.go:FDKaacEncInitElementBits` | source-shaped partial port | Source-shaped CBR and VBR multi-element QC init vectors, FF reservoir-preservation/reset vector, malformed-control tests, no-cgo compile gate, and zero steady-state allocation check | Covers FDK's `FDKaacEnc_QCInit` kernel budget/reservoir state transfer, element-bit initialization, VBR quality-factor selection, LC-safe dead-zone flag calculation, and threshold-state seeding through channel-map relative-bit traversal. The broader `aacenc.cpp` bitrate-limit/max-frame setup, psychoacoustic initialization, low-delay advanced bits-to-PE interpolation table, public encoder facade, and oracle byte parity are not claimed yet. |
| FDK AAC-LC frame bitrate padding | `internal/fdkaac/qc_main.go:FDKaacEncCalcFrameLen`, `FDKaacEncFramePadding`, `FDKaacEncAdjustBitrate`; `internal/fdkaac/qc_main.go:QCKernel.PaddingRest` | source-shaped partial port | Source-shaped frame-byte integer/modulo vectors, repeated padding-rest transition vector, config/QC-init prepared-kernel handoff vector, malformed-control tests, no-cgo compile gate, and zero steady-state allocation check | Covers FDK's per-frame byte padding and `avgTotalBits` update before `FDKaacEnc_QCMain`, including the persistent `paddingRest` state carried from QC init. Dynamic transport static-bit refresh, final access-unit write, full encode-frame orchestration, and public encoder facade are not claimed yet. |
| FDK AAC-LC QC prepare wrapper | `internal/fdkaac/qc_main.go:FDKaacEncQCMainPrepare`; `internal/fdkaac/bitenc.go:FDKaacEncChannelElementWrite` nil-QC dry run | source-shaped partial port | Wrapper vector compares `FDKaacEnc_QCMainPrepare` against the direct form-factor, PE-calculation, and static channel-element dry-run sequence; SCE static dry-run bit-count vector, malformed transition tests, no-cgo compile gate, and zero steady-state allocation check | Covers FDK's prepare stage before bit distribution and quantization: form-factor refresh, unreduced PE calculation, and static side-info bit demand. The full `FDKaacEnc_QCMain` quantization loop, final access-unit packing, and public encoder facade are not claimed yet. |
| FDK AAC-LC QC quantize/reduce loop subset | `internal/fdkaac/qc_main.go:ElementBits`, `FDKaacEncCalcMaxValueInSfb`, `FDKaacEncReduceBitConsumption`, `FDKaacEncCrashRecovery`, `FDKaacEncUpdateUsedDynBits`, `FDKaacEncGetTotalConsumedDynBits`, `FDKaacEncQCMainQuantizeFrame`; `internal/fdkaac/adj_thr.go:QCOut.UsedDynBits` | source-shaped partial port | Source-shaped max-value vector, direct-sequence comparison for SCE scale-factor estimation, spectrum quantization, max-value scan, dynamic noiseless bit count, `dynBitsLast` seeding, and frame dynamic-bit summation; reduce-bit normal gain-adjustment and max-iteration non-crash vectors; constraint-reset re-entry vector covering the next-pass gain/iteration transition; crash-recovery zero-spectrum vector covering high-band trimming, TNS/tool clearing, and static-bit rebudgeting; stale scratch reset test; malformed transition tests, no-cgo compile gate, and zero steady-state allocation check | Covers the quantize/count loop body of `FDKaacEnc_QCMain` after threshold adjustment plus the source-shaped `FDKaacEnc_reduceBitConsumption` and emergency `FDKaacEnc_crashRecovery` branches for one-subframe AAC-LC elements. Final access-unit packing and the public encoder facade are not claimed yet. |
| FDK AAC-LC QC fill/finalize/reservoir accounting subset | `internal/fdkaac/qc_main.go:QCBitrateMode`, `QCKernel`, `FDKaacEncGetTotalConsumedBits`, `FDKaacEncUpdateFillBits`, `FDKaacEncFinalizeBitConsumption`, `FDKaacEncUpdateBitres`; `internal/fdkaac/adj_thr.go:QCOut` accounting fields | source-shaped partial port | Source-shaped total consumed-bit vector over SCE/CPE/DSE mapping, VBR and CBR fill-bit vectors, static-transport finalization vector covering fill-extension sizing and byte alignment, CBR/VBR bit-reservoir update vectors, malformed transition tests, no-cgo compile gate, and zero steady-state allocation check | Covers FDK's post-quantization bit accounting for one-subframe AAC-LC with caller-supplied static transport header bits. The dynamic `transportEnc_GetStaticBits` callback shape, full total-frame balancing loop, actual fill/alignment bitstream emission into the final AU, reservoir redistribution before thresholding, and public encoder facade are not claimed yet. |
| FDK AAC-LC QC bit-distribution/reservoir redistribution subset | `internal/fdkaac/qc_main.go:FDKaacEncDistributeElementDynBits`, `FDKaacEncGetMinimalStaticBitDemand`, `FDKaacEncPrepareBitDistribution`, `FDKaacEncBitResRedistribution`, `QCKernel`, `ElementBits` | source-shaped partial port | Source-shaped element dynamic-bit grant vector, reservoir redistribution vector, one-SCE prepare-distribution vector through the already-ported PE grant helper, minimal static bit-demand vector, low-reservoir negative granted-dynamic-bits transition vector, malformed transition tests, no-cgo compile gate, and zero steady-state allocation check | Covers one-subframe AAC-LC dynamic-bit grant setup, per-element reservoir level redistribution, minimal static-demand accounting, and PE grant handoff before threshold adjustment/quantization. The full outer `FDKaacEnc_QCMain` loop, final access-unit packing, and public encoder facade are not claimed yet. |
| FDK AAC-LC QC one-frame control wrapper subset | `internal/fdkaac/qc_main.go:FDKaacEncQCMainFrame`, `FDKaacEncQCMainQuantizationDecision`, `FDKaacEncCheckMinFrameBitsDemand`, `QCMainFrameScratch` | source-shaped partial port | QC wrapper vector covering reservoir redistribution, bit distribution, threshold adjustment, repeated quantize/count staging, constraint reset, and post-quantization decision; branch vectors for under-budget exit, over-budget saving, dynamic-bit overshoot, and emergency-iteration spending; malformed transition tests, no-cgo compile gate, and zero steady-state allocation check | Covers FDK's one-subframe setup, repeated frame-control loop wiring, and frame-control decision ordering around the already-ported QC helpers. Multi-subframe handling, final fill/alignment emission into an AU, and public encoder facade are not claimed yet. `checkMinFrameBitsDemand` remains a validated no-op because the pinned FDK source returns success unconditionally. |
| FDK AAC-LC short grouping subset | `internal/fdkaac/group_short.go` | source-shaped partial port | Source-derived vectors for `FDKaacEnc_groupShortData` grouped offsets, min-SNR propagation, saturating threshold/energy/MS/spread-energy sums, max-SFB scan, zero-spectrum max-SFB floor, active and inactive-band spectrum regrouping, invalid transition tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers AAC-LC short-window grouping after short MDCT/band energy, including inactive high-band grouping used by lowpass/bandwidth controls. Go zeroes skipped inactive spectrum slots before copying from caller-owned scratch, because the C source leaves those ignored scratch slots unwritten; full `BuildInterface`, threshold adaptation, TNS, quantization, and bitstream coding are not claimed yet. |
| FDK AAC-LC threshold spreading subset | `internal/fdkaac/spreading.go` | source-shaped partial port | Source-derived vectors for `FDKaacEnc_SpreadingMax`, invalid transition tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers bidirectional max spreading over already-computed threshold/energy bands. Psy configuration factor generation, full threshold adaptation, and PE are not claimed yet. |
| FDK AAC-LC pre-echo control subset | `internal/fdkaac/pre_echo.go` | source-shaped partial port | Source-derived vectors for `FDKaacEnc_InitPreEchoControl` and `FDKaacEnc_PreEchoControl` disabled/current-scale-larger/previous-scale-larger branches, invalid state/shift tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers the threshold history limiter used during psychoacoustic threshold advance. Full threshold clipping, PCM quantization thresholding, TNS coupling, and complete `FDKaacEnc_psyMain` integration are not claimed yet. |
| FDK AAC-LC threshold advance subset | `internal/fdkaac/threshold.go` | source-shaped partial port | Source-derived vectors for the `psy_main.cpp` "Advance thresholds" block: clip-energy scaling, threshold spreading, positive and negative PCM quantization floor shifts, FDK's in-loop short-window `energyShift` mutation, start/stop pre-echo history transitions, spread-energy preparation, invalid transition tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers threshold advancement after band energy and before MS/grouping output preparation. Psy configuration generation, TNS, PNS, MS/intensity stereo, quantization, noiseless coding, and final bitstream assembly are not claimed yet. |
| FDK AAC-LC MS stereo subset | `internal/fdkaac/ms_stereo.go` | source-shaped partial port | Source-derived vectors for `FDKaacEnc_MsStereoProcessing` covering MS-none, MS-some with intensity-stereo exclusions, MS-all promotion, per-band mask/digest decisions, mid/side spectrum rewrites, energy/threshold/LD/spread updates, invalid transition tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers the M/S stereo processing block after psychoacoustic threshold and energy preparation. Intensity stereo decision/scales, PNS preprocessing/postprocessing, TNS coupling, quantization, noiseless coding, and bitstream writing are not claimed yet. |
| FDK AAC-LC psychoacoustic output handoff subset | `internal/fdkaac/psy_output.go` | source-shaped partial port | Source-derived vectors for the `psy_main.cpp` "build output" block covering long/short `sfbCnt`, `sfbPerGroup`, `maxSfbPerGroup`, MDCT scale, window sequence/shape selection, grouping-mask generation, group-length copy, grouped energy/spread-energy copies, invalid transition tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers the final psychoacoustic channel-output handoff shape consumed by later quantization. It does not include PNS coding, TNS serialization, quantization, scale-factor estimation, noiseless coding, or final bitstream assembly. |
| FDK AAC-LC form-factor subset | `internal/fdkaac/sf_estim.go:FDKaacEncCalcFormFactor` | source-shaped partial port | Source-derived vectors for `FDKaacEnc_CalcFormFactor` over long and grouped-short channel layouts, inactive-band sentinel checks, invalid channel/output/offset/spectrum transition tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers the `sf_estim.cpp` form-factor input to scale-factor estimation. Scale-factor search, quantization, noiseless coding, rate control, and final bitstream writing are not claimed yet. |
| FDK AAC-LC relevant-lines subset | `internal/fdkaac/sf_estim.go:FDKaacEncCalcSfbRelevantLines` | source-shaped partial port | Source-derived vectors for `FDKaacEnc_calcSfbRelevantLines` over long and grouped-short layouts, output-clearing checks, active-band energy/threshold gating, malformed input transition tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers the relevant-line estimate consumed by scale-factor search. Scale-factor search, quantization, noiseless coding, rate control, and final bitstream writing are not claimed yet. |
| FDK AAC-LC single-band PE subset | `internal/fdkaac/sf_estim.go:FDKaacEncCountSingleScfBits`, `FDKaacEncCalcSingleSpecPe` | source-shaped partial port | Source-derived vectors for single-band scalefactor bit cost and spectral PE cost, high/low `ldRatio` PE branches, out-of-range delta transitions through the Huffman length helper, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers the scalar helpers used inside `sf_estim.cpp` scale-factor search. Multi-band PE diffs, improve-SCF search, quantization, noiseless coding, rate control, and final bitstream writing are not claimed yet. |
| FDK AAC-LC multi-band PE diff subset | `internal/fdkaac/sf_estim.go:FDKaacEncCountScfBitsDiff`, `FDKaacEncCalcSpecPeDiff` | source-shaped partial port | Source-derived vectors for multi-band scalefactor bit-cost deltas and spectral PE deltas, previous/next relevant-band linking, skipped `FDK_INT_MIN` bands, lazy `sfbConstPePart` fill verification, invalid transition tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers the local delta-cost helpers used by scale-factor refinement loops. The outer `FDKaacEnc_improveScf` search, quantizer distortion, noiseless coding, rate control, and final bitstream writing are not claimed yet. |
| FDK AAC-LC improve-SCF subset | `internal/fdkaac/sf_estim.go:FDKaacEncImproveScf` | source-shaped partial port | Source-derived vectors for accepted upward and downward scale-factor refinement, minimum-scale stopping, dead-zone quantization, quantized-spectrum mutation, invalid transition tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers the inner best-scale-factor refinement helper around quantizer distortion. Outer assimilation loops, noiseless coding, bit reservoir control, rate control, and final bitstream writing are not claimed yet. |
| FDK AAC-LC single-SCF assimilation subset | `internal/fdkaac/sf_estim.go:FDKaacEncAssimilateSingleScf` | source-shaped partial port | Source-derived vectors for neighbouring-scale constrained refinement, restart-on-success rescans, already-checked minimum-scale controls, no-op flat-scale control, quantized-spectrum mutation, invalid transition tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers the single-band scale-factor assimilation loop after initial quantizer distortion feedback. Multi-SCF assimilation, final scale-factor estimation orchestration, noiseless coding, bit reservoir control, rate control, and final bitstream writing are not claimed yet. |
| FDK AAC-LC multi-SCF assimilation subset | `internal/fdkaac/sf_estim.go:FDKaacEncAssimilateMultipleScf` | source-shaped partial port | Source-derived vectors for regional shared-scale lowering, threshold rejection, minimum-scale rejection, irrelevant-band no-op control, quantized/temp-spectrum mutation, invalid transition tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers the first multi-band scale-factor assimilation loop after initial quantizer distortion feedback. The second multi-SCF assimilation variant, final scale-factor estimation orchestration, noiseless coding, bit reservoir control, rate control, and final bitstream writing are not claimed yet. |
| FDK AAC-LC multi-SCF assimilation-2 subset | `internal/fdkaac/sf_estim.go:FDKaacEncAssimilateMultipleScf2` | source-shaped partial port | Source-derived vectors for coarser regional refinement, finer regional refinement, no-quant scale lowering, threshold rejection, irrelevant-band no-op control, invalid transition tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers the second multi-band scale-factor assimilation loop after initial quantizer distortion feedback. Final scale-factor estimation orchestration, noiseless coding, bit reservoir control, rate control, and final bitstream writing are not claimed yet. |
| FDK AAC-LC channel scale-factor estimation subset | `internal/fdkaac/sf_estim.go:FDKaacEncEstimateScaleFactorsChannel` | source-shaped partial port | Source-derived vectors for initial scale-factor estimation, max-quant lower bounds, inverse-quant refinement, single/multi-SCF orchestration, dead-zone quantization, all-zero spectrum handling, grouped inactive-band transitions, invalid transition tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers the per-channel `sf_estim.cpp` scale-factor orchestration and quantized-spectrum side effects. FDK's scratch allocation is replaced by caller-owned `quantSpecTmp`, and C's unrolled spectrum maximum scan/1024 clear are slice-bounded in Go. Top-level channel/pair iteration, noiseless coding, bit reservoir control, rate control, and final bitstream writing are not claimed yet. |
| FDK AAC-LC multi-channel scale-factor wrapper | `internal/fdkaac/sf_estim.go:QCOutChannel`, `FDKaacEncEstimateScaleFactors` | source-shaped partial port | Source-derived vectors for FDK channel-owned `quantSpec`, `scf`, and `globalGain` state, two-channel active/all-zero iteration, shared scratch reuse, invalid transition tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers only the `sf_estim.cpp` channel walker above the per-channel estimator. The Go port exposes caller-owned scratch instead of C scratch allocation; element/channel mapping, noiseless coding, bit reservoir control, rate control, and final bitstream writing are not claimed yet. |
| FDK AAC-LC line-PE subset | `internal/fdkaac/fixlog.go:CalcLdInt`, `internal/fdkaac/fixpoint.go:FMultI`, `internal/fdkaac/line_pe.go:FDKaacEncPrepareSfbPe`, `FDKaacEncCalcSfbPe` | source-shaped partial port | Source-derived vectors for the FDK integer-LD table, `fMultI` rounding, active-line preparation, line-PE high/low branches, inactive/intensity-book transitions, invalid transition tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers the per-channel perceptual entropy preparation used by bit allocation and threshold adjustment. PE calculation is covered separately; bit reservoir control, quantizer distortion, noiseless coding, and final bitstream writing are not claimed yet. |
| FDK AAC-LC threshold-adjustment initialization | `internal/fdkaac/adj_thr.go:FDKaacEncInitAdjThrState`, `FDKaacEncInitATSElement`, `FDKaacEncInitBits2PeFactor` | source-shaped partial port | Source-derived vectors for `FDKaacEnc_AdjThrInit` common state mode selection, second-guess iteration count, FDK bit-reservoir constants, per-element PE min/max bounds, low-bitrate PE offset, avoid-hole thresholds, min-SNR defaults, PE-correction defaults, default `FDKaacEnc_InitBits2PeFactor` output, stale-state reset, malformed controls, no-cgo compile gate, and zero steady-state allocation check | Covers deterministic single-element/common AAC-LC threshold-adjustment seeding before PE adaptation and bit distribution. FDK multi-element `CHANNEL_MAPPING` relative-bits traversal and the AAC-LD advanced bits-to-PE table interpolation branch are not claimed; the unsupported advanced branch fails loudly for one- and two-channel use instead of returning a fake factor. |
| FDK AAC-LC threshold-adjustment wrapper | `internal/fdkaac/adj_thr.go:FDKaacEncAdjustThresholds`, `QCOut`, `QCOutElement`, `ChannelMapping`; `internal/fdkaac/bitenc.go:ElementInfo`, `PsyOutElement` | source-shaped partial port | CBR and VBR wrapper vectors compare `FDKaacEnc_AdjustThresholds` branch wiring against the already-ported inner threshold adapters, including a two-element global inter-CBR vector, final threshold unweighting, malformed mapping/control tests, no-cgo compile gate, and zero steady-state allocation check | Covers the source-shaped adjustment wrapper for single-element CBR, intra-element CBR loops, inter-element maximum-element-PE fallback, global multi-element inter-CBR reduction across adjustable elements, VBR per-element traversal, and post-adjustment threshold unweighting. Public encoder facade integration, runtime full-loop oracle byte parity, and final access-unit production are not claimed yet. |
| FDK AAC-LC min-SNR adaptation subset | `internal/fdkaac/adj_thr.go:FDKaacEncCalcThresholdExp`, `FDKaacEncAdaptMinSnr`, `FDKaacEncInitMinSnrAdaptParam` | source-shaped partial port | Source-derived threshold-exponent vector, default min-SNR adaptation constants, low-energy min-SNR reduction vector, zero-energy no-op control, invalid transition tests, no-cgo compile gate, and zero steady-state allocation check | Covers threshold exponent scratch preparation and FDK's frame-average-energy min-SNR relaxation before threshold reduction. Second-guess iteration and public encoder integration are not claimed yet. |
| FDK AAC-LC avoid-hole initialization subset | `internal/fdkaac/adj_thr.go:FDKaacEncInitAvoidHoleFlag` | source-shaped partial port | Source-derived long-window, short-window, and stereo-MS vectors for spread-energy attenuation, local peak/valley min-SNR edits, MS min-SNR/spread coupling, avoid-hole flag state output, inactive grouped-band preservation, invalid transition tests, no-cgo compile gate, and zero steady-state allocation check | Covers FDK's `FDKaacEnc_initAvoidHoleFlag` over already-computed psychoacoustic and quantization-control channel data. The later avoid-hole relaxation fallback and the partial outer `FDKaacEnc_AdjustThresholds` wrapper are covered separately; multi-element global inter-CBR adjustment, quantizer iteration, final bitstream writing, and public encoder facade are not claimed yet. |
| FDK AAC-LC pow2 reduction helper | `internal/fdkaac/fixlog.go:f2Pow`, `internal/fdkaac/adj_thr.go:FDKaacEncCalcRedValPower` | source-shaped partial port | Source-derived `pow2Coeff` table edge checks, mantissa/exponent vectors across positive, negative, re-centered, and split-exponent branches, CBR-style `CalcRedValPower` vectors, malformed controls, no-cgo compile gate, and zero steady-state allocation check | Covers FDK's fixed-point `2^x` primitive and the reduction-value power helper used by the first and second CBR threshold guesses. The full `FDKaacEnc_adaptThresholdsToPe` iteration, multi-element traversal, and public encoder integration are not claimed yet. |
| FDK AAC-LC CBR PE-adaptation loop subset | `internal/fdkaac/adj_thr.go:FDKaacEncAdaptThresholdsToPeCBR` | source-shaped partial port | Source-derived first-guess and second-guess reduction-value vectors, malformed orchestration controls, caller-owned scratch, no-cgo compile gate, and zero steady-state allocation coverage for the reduction math | Covers the single-element CBR ordering around already-ported threshold exponent, min-SNR adaptation, avoid-hole initialization, CBR threshold reduction, no-AH PE summation, threshold correction, min-SNR fallback, avoid-hole relaxation, and AH reset helpers. The outer wrapper now covers global multi-element inter-CBR traversal separately; a runtime full-loop oracle vector, VBR threshold adaptation, quantizer iteration, and public encoder integration are not claimed yet. |
| FDK AAC-LC CBR threshold-reduction subset | `internal/fdkaac/adj_thr.go:FDKaacEncCalcPENoAH`, `FDKaacEncReduceThresholdsCBR` | source-shaped partial port | Source-derived no-AH PE summation vector, CBR threshold-reduction vectors for AH-active skips, NO_AH bypass, AH activation/clamping, 29 dB minimum ratio limiting, zero-reduction no-op, invalid transition tests, no-cgo compile gate, and zero steady-state allocation check | Covers FDK's CBR threshold-reduction formula and the PE summation used by second-guess iteration over already prepared PE/threshold/avoid-hole state. Final threshold correction, min-SNR PE reduction, and avoid-hole relaxation are covered separately; the outer `FDKaacEnc_AdjustThresholds` loop and public encoder integration are not claimed yet. |
| FDK AAC-LC VBR threshold-reduction subset | `internal/fdkaac/adj_thr.go:FDKaacEncCalcChaosMeasure`, `FDKaacEncReduceThresholdsVBR` | source-shaped partial port | Source-derived inverse-table hashes for `invInt` and `invSqrt4`, chaos-measure vector and no-active control, long-window and short-window VBR threshold-reduction vectors, malformed controls, no-cgo compile gate, and zero steady-state allocation check | Covers FDK's VBR threshold reduction over prepared PE/threshold/avoid-hole state, including chaos smoothing/history and short-window group scaling. Final threshold correction, min-SNR PE reduction, and avoid-hole relaxation are covered separately; the outer `FDKaacEnc_AdjustThresholds` loop and public encoder integration are not claimed yet. |
| FDK AAC-LC VBR threshold-adaptation wrapper | `internal/fdkaac/adj_thr.go:FDKaacEncAdaptThresholdsVBR` | source-shaped partial port | Source-shaped ordering around threshold exponent, min-SNR adaptation, avoid-hole initialization, and VBR threshold reduction; malformed orchestration controls, caller-owned scratch, no-cgo compile gate, and zero steady-state allocation guard | Covers FDK's single-element `FDKaacEnc_AdaptThresholdsVBR` wrapper over already-prepared psychoacoustic and quantization-control state. Runtime full-wrapper vectors, multi-element `FDKaacEnc_AdjustThresholds` traversal, quantizer iteration, and public encoder integration are not claimed yet. |
| FDK AAC-LC correct-threshold subset | `internal/fdkaac/adj_thr.go:FDKaacEncCorrectThresholds` | source-shaped partial port | Source-shaped vectors for `FDKaacEnc_correctThresh` with positive and negative PE deltas, PE-factor scratch hashes, active-line LD scratch hashes, AH-inactive clamp controls, malformed prepared-state controls, no-cgo compile gate, and zero steady-state allocation check | Covers the single-element prepared threshold/PE/avoid-hole state used by the local channel-array APIs. FDK multi-element `CHANNEL_MAPPING` traversal, the outer `FDKaacEnc_AdjustThresholds` loop, quantizer iteration, and public encoder integration are not claimed yet. |
| FDK AAC-LC min-SNR PE-reduction subset | `internal/fdkaac/adj_thr.go:FDKaacEncReduceMinSnr` | source-shaped partial port | Source-shaped vector for `FDKaacEnc_reduceMinSnr` covering descending high-SFB traversal, min-SNR relaxation to 1 dB, threshold replacement, PE-band replacement with `1.5 * sfbNLines`, global/channel PE updates, no-op control, malformed prepared-state controls, no-cgo compile gate, and zero steady-state allocation check | Covers the single-element prepared PE/threshold/avoid-hole fallback used when desired PE is still not reached. FDK multi-element `CHANNEL_MAPPING` traversal, the outer `FDKaacEnc_AdjustThresholds` loop, quantizer iteration, and public encoder integration are not claimed yet. |
| FDK AAC-LC allow-more-holes subset | `internal/fdkaac/adj_thr.go:FDKaacEncAllowMoreHoles` | source-shaped partial port | Source-shaped M/S vector for lower-energy side-channel hole opening and inactive-band PE clearing, source-shaped energy-fallback vector for low-energy high-band erasure, no-op control, malformed prepared-state controls, no-cgo compile gate, and zero steady-state allocation check | Covers the single-element prepared PE/threshold/avoid-hole fallback used after threshold correction and min-SNR reduction. FDK multi-element `CHANNEL_MAPPING` traversal, the outer `FDKaacEnc_AdjustThresholds` loop, quantizer iteration, and public encoder integration are not claimed yet. |
| FDK AAC-LC AH reset subset | `internal/fdkaac/adj_thr.go:FDKaacEncResetAHFlags` | source-shaped partial port | Source-shaped vector for `FDKaacEnc_resetAHFlags` covering `AH_ACTIVE` to `AH_INACTIVE` transitions only inside active SFB ranges, inactive-band preservation, malformed prepared-state controls, no-cgo compile gate, and zero steady-state allocation check | Covers the avoid-hole flag reset used when the second-guess loop overshoots and scales reduction back upward. FDK multi-element `CHANNEL_MAPPING` traversal, the full `FDKaacEnc_adaptThresholdsToPe` loop, and public encoder integration are not claimed yet. |
| FDK AAC-LC PE threshold-adjustment subset | `internal/fdkaac/fixlog.go:fDivNorm`, `internal/fdkaac/adj_thr.go:FDKaacEncPECalculation` | source-shaped partial port | Source-derived vectors for normalized Schur division, PE preparation, long-window weighting-patch execution, weighted energy/threshold mutation, short-window patch-state transition, invalid transition tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers PE calculation and the FDK energy-weighting patch over already-computed psychoacoustic and quantization-control channel data. Bit distribution, reservoir correction, multi-element `FDKaacEnc_AdjustThresholds`, and public encoder integration are not claimed yet. |
| FDK AAC-LC bit-reservoir PE distribution subset | `internal/fdkaac/adj_thr.go:FDKaacEncInitBitresState`, `FDKaacEncBitresCalcBitFac`, `FDKaacEncDistributeBits` | source-shaped partial port | Source-derived vectors for long/short bit-reservoir factors, full/reduced/disabled bit distribution, high-reservoir and low-reservoir PE-correction controls, invalid transition tests, no-cgo compile gate, and zero steady-state allocation check | Covers FDK's element-local dynamic-bit to PE grant decision and PE correction over already-computed PE and psychoacoustic window state. The outer second-guess loop, avoid-hole threshold reduction, quantizer iteration, multi-element allocation policy, final access-unit packing, and public encoder facade are not claimed yet. |
| FDK AAC-LC quantization subset | `internal/fdkaac/quant_tables.go`, `internal/fdkaac/quantize.go` | source-shaped partial port | Mechanically derived checksums for `FDKaacEnc_mTab_3_4`, `FDKaacEnc_mTab_4_3Elc`, quantizer Q/E tables, and exponent-combination tables; source-derived vectors for quantize/inverse-quantize, grouped `FDKaacEnc_QuantizeSpectrum`, `FDKaacEnc_calcSfbDist`, `FDKaacEnc_calcSfbQuantEnergyAndDist`, invalid transition tests, and zero steady-state allocation check under `GOOS=js GOARCH=wasm` | Covers quantized spectrum generation and distortion feedback used by scale-factor estimation. Huffman/noiseless coding, quantizer iteration policy, bit reservoir control, and final bitstream writing are not claimed yet. |
| FDK AAC-LC transport emission | `internal/fdkaac/transport.go` | source-shaped partial port | Native FDK v2.0.3 byte-vector probe for ASC/ADTS, unit tests, unsupported-branch tests, and zero steady-state allocation check | AAC-LC mono/stereo ASC and CRC-less single-raw-block ADTS only; PCE, SBR/PS, ER tools, CRC, LATM/LOAS, and ADIF are not claimed yet. |
| AAC-LC vector corpus | `testdata/aaclc`, `vectors_test.go` | oracle fixture suite | byte-exact PCM SHA-256 checks across one-shot, streaming, frame-by-frame, and raw-payload APIs | Vectors are generated ADTS AAC-LC streams; PCM hashes are produced by pinned FAAD2. |
| Public decoder facade | `decoder.go`, `adts_reader.go` | Go facade | unit tests, committed vectors, and native FAAD2 oracle byte comparison | Exposes explicit raw/ADTS transports, append-style decode APIs, frame metadata, and streaming ADTS reads. |
